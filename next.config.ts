import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ['sinless-shut-undoing.ngrok-free.dev'],
};

export default nextConfig;
